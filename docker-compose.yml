version: "3.9" 
services:
  web:
    build: .
    ports:
      - "8000:5000"
    volumes:
      - .:/code
      - mekilu:/var/log
    links:
      - redis
  redis:
    image: redis
volumes:
  mekilu: {}