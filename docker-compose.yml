version: "3.9" 
services:
  web:
    build: .
    ports:
      - "8000:5000"
    volumes:
      - .:/code
      - memek:/var/log
    links:
      - redis
  redis:
    image: redis
volumes:
  memek: {}
